# Changelog

## 1.0.0

- Initial release
- Player health bar notches every 5 max HP (configurable via `PlayerHealthPerNotch`)
- Boss / abomination / floating enemy notches every 500 max HP (configurable via `EnemyHealthPerNotch`)
- Configurable intervals, color, thickness, and feature toggles

